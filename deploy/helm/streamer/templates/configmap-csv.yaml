{{- if .Values.csv.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "streamer.fullname" . }}-csv
  labels:
    {{- include "streamer.labels" . | nindent 4 }}
data:
  telemetry.csv: |
{{ .Files.Get "files/telemetry.csv" | indent 4 }}
{{- end }}
