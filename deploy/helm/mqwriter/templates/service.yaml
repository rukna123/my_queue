apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqwriter.fullname" . }}
  labels:
    {{- include "mqwriter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mqwriter.selectorLabels" . | nindent 4 }}
