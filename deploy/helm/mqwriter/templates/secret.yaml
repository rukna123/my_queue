apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mqwriter.fullname" . }}
  labels:
    {{- include "mqwriter.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
