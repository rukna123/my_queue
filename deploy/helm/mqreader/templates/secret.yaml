apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mqreader.fullname" . }}
  labels:
    {{- include "mqreader.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
