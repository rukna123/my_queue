apiVersion: v1
kind: Service
metadata:
  name: {{ include "partition-controller.fullname" . }}
  labels:
    {{- include "partition-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "partition-controller.selectorLabels" . | nindent 4 }}
