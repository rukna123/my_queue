apiVersion: v1
kind: Service
metadata:
  name: {{ include "prompted-apigw.fullname" . }}
  labels:
    app.kubernetes.io/name: prompted-apigw
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: prompted-apigw
    app.kubernetes.io/instance: {{ .Release.Name }}
